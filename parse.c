#include "shell.h"

/* Parse args to commands */
void parse_args(char **args, command_t *commands)
{
    // char *token;
    // int i = 0;

    // command_t *cur = commands;
    // command_t new_cmd;

    // enum parse_flag {RED_IN, RED_OUT, RED_ERR, PIPE}

    // token = args[i];
    // while (token != NULL) {
    //     DEBUG_PRINT("token: %s", token);

    //     switch (token[0]) {
    //         case '<':
    //             break;
                

    //     }

    //     token = args[++i];
    // }
}